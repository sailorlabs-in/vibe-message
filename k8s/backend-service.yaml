apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: message-app
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
    - port: 3300
      targetPort: 3300
      protocol: TCP
      name: http
  selector:
    app: backend
